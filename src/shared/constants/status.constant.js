export const resetActionStatus = {
  submitted: false,
  success: false,
  loading: false,
}

export const initActionStatus = {
  submitted: true,
  success: false,
  loading: true,
}

export const successActionStatus = {
  submitted: true,
  success: true,
  loading: false,
}

export const failedActionStatus = {
  submitted: true,
  success: false,
  loading: false,
}
